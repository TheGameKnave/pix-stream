{
  "name": "photo-stream",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm run client\" \"npm run server\"",
    "client": "cd client && ng serve --configuration development",
    "server": "/opt/homebrew/bin/php -S localhost:8080 -t server/ server/router.php",
    "build": "cd client && npm run build",
    "build:prod": "cd client && npm run build -- --configuration production",
    "test": "cd client && npm test",
    "test:php": "cd server && php phpunit.phar --testdox",
    "test:all": "npm test && npm run test:php",
    "prepare": "npx --yes is-ci || husky || true"
  },
  "workspaces": [
    "./client"
  ],
  "devDependencies": {
    "@types/compression": "^1.8.1",
    "@types/qrcode": "^1.5.6",
    "concurrently": "^9.1.2",
    "husky": "^9.1.7",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "marked": "^17.0.5",
    "ngx-markdown": "^21.1.0",
    "qrcode": "^1.5.4"
  }
}
